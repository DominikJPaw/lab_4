name: Python package

on: [push]

jobs:
  build:

    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest]
        python version: ["3.9","3.10","3.11","pypy3.10","pypy3.9"]
        exclude:
        - os: macos-latest
          python latest: "3.9"
